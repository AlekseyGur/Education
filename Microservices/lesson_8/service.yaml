apiVersion: v1
kind: Service
metadata:
  name: lesson8
spec:
  selector:
    app: app
  ports:
    - port: 8000
      targetPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: lesson8DB
spec:
  ports:
  - port: 5432
    targetPort: 5432
  selector:
    app: lesson8DB
