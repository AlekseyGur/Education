---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: lesson8DB
spec:
  replicas: 1
  serviceName: lesson8DB
  selector:
    matchLabels:
      app: lesson8DB
  template:
    metadata:
      labels:
        app: lesson8DB
    spec:
      containers:
        - image: postgres:10.1
          name: lesson8DB
          env:
            - name: POSTGRES_USER
              value: app
            - name: POSTGRES_DB
              value: users
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app
                  key: db-password
          ports:
          - containerPort: 5432
            protocol: TCP
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 2Gi
        storageClassName: msk-hdd1
